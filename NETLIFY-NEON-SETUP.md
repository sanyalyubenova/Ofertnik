# Netlify + Neon Database Setup

## Claim Database через Netlify

Ако успешно claim-нахте Neon database през Netlify, това означава:

1. ✅ Netlify автоматично управлява `DATABASE_URL` environment variable
2. ✅ При deployment, Netlify предоставя connection string-а автоматично
3. ✅ Не е необходимо да задавате `DATABASE_URL` ръчно

## Как работи

### Приложение вече е готово

Кодът в `server.js` и `db/connection.js` вече е конфигуриран да:
- Използва `process.env.DATABASE_URL` ако е наличен
- Автоматично fallback към файлова система ако не е наличен

### Local Development

За локална разработка, може да:
1. Използвате `.env` файл с `DATABASE_URL` (ако искате да тествате с базата данни локално)
2. Или да тествате с файлова система (без `.env` файл)

### Production (Netlify)

При deployment в Netlify:
- Netlify автоматично предоставя `DATABASE_URL` от claim-натата база
- Не е необходимо допълнително конфигуриране
- Просто deploy-нете и всичко работи!

## Проверка

След deployment в Netlify, можете да проверите:
1. Netlify Dashboard → Environment variables → виждате `DATABASE_URL`
2. Logs показват "📦 Using Neon PostgreSQL database"
3. API endpoints работят с базата данни

## Миграция на данни

Ако искате да мигрирате данни в claim-натата база:

1. **Локално мигриране:**
   ```bash
   # Задайте DATABASE_URL в .env файл (от Netlify Dashboard)
   # След това:
   node scripts/migrate-data.js
   ```

2. **Или използвайте Netlify CLI:**
   ```bash
   netlify dev:exec node scripts/migrate-data.js
   ```

## Бележки

- Claim-натата база в Netlify е **production** базата
- Не се препоръчва да я използвате за разработка (използвайте отделна Neon база за dev)
- Всички environment variables са видими в Netlify Dashboard

