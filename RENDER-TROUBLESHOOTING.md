# Решаване на проблеми с Render - Login не работи

## Проблем: Не мога да се логна в приложението на Render

Ето какво да провериш:

---

## 1. Environment Variables

Най-често проблемът е от липсващи или неправилни environment variables.

### Проверка в Render Dashboard:

1. Отиди на твоя Web Service в Render
2. Кликни "Environment"
3. Провери дали имаш:

| Key | Value | Важно |
|-----|-------|-------|
| `DATABASE_URL` | `postgresql://...` | Твоя Neon DB connection string |
| `SESSION_SECRET` | `дълъг random string` | Трябва да е зададен! |
| `NODE_ENV` | `production` | Важно за sessions |

### Как да добавиш/редактираш:

1. Render Dashboard → твоя Web Service → Environment
2. Кликни "Add Environment Variable"
3. Добави всеки variable
4. Кликни "Save Changes"
5. Render автоматично ще рестартира сървъра

---

## 2. Създаване на Admin Потребител

Ако нямаш admin акаунт, трябва да го създадеш.

### Опция A: Локално (препоръчително)

1. На локалния компютър, отвори терминала
2. Увери се, че `.env` файлът има правилен `DATABASE_URL` (същия като в Render)
3. Изпълни:

```bash
node scripts/create-admin.js
```

Това ще създаде admin потребител в базата данни (която се споделя с Render).

### Опция B: Чрез Render Shell (по-сложно)

1. Render Dashboard → твоя Web Service → Shell
2. Изпълни:

```bash
node scripts/create-admin.js
```

**Проблем:** Може да нямаш достъп до scripts/ директорията или DATABASE_URL може да не е зададен.

---

## 3. Проверка на Database Connection

Провери дали базата данни работи:

### В Render Logs:

1. Render Dashboard → твоя Web Service → Logs
2. Търси за съобщения като:
   - `📦 Using Neon PostgreSQL database` - ✅ Работи
   - `Database connection failed` - ❌ Проблем
   - `DATABASE_URL environment variable is not set` - ❌ Липсва DATABASE_URL

### Локално тест:

```bash
# На локалния компютър
node scripts/test-connection.js
```

---

## 4. Session Configuration

Провери дали sessions работят правилно.

В `server.js`, session конфигурацията трябва да изглежда така:

```javascript
app.use(session({
    secret: process.env.SESSION_SECRET || 'default-secret-change-in-production',
    resave: false,
    saveUninitialized: false,
    cookie: {
        secure: process.env.NODE_ENV === 'production', // HTTPS only in production
        httpOnly: true,
        maxAge: 24 * 60 * 60 * 1000 // 24 hours
    }
}));
```

**Важно:** 
- `SESSION_SECRET` трябва да е зададен в Render
- `NODE_ENV=production` активира `secure: true` (HTTPS cookies)

---

## 5. Проверка на Login Endpoint

Провери Render Logs за грешки при login:

1. Render Dashboard → Logs
2. Опитай да се логнеш
3. Виж дали има грешки в логовете

Често срещани грешки:
- `Database connection failed`
- `User not found`
- `Invalid password`
- `SESSION_SECRET is not set`

---

## 6. Стъпка по стъпка решение

### Стъпка 1: Провери Environment Variables

```bash
# В Render Dashboard → Environment, провери:
DATABASE_URL=postgresql://... (правилен Neon DB URL)
SESSION_SECRET=дълъг-random-string (генериран)
NODE_ENV=production
```

### Стъпка 2: Генерирай нов SESSION_SECRET (ако нямаш)

```bash
# Локално
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

Копирай резултата и го добави в Render като `SESSION_SECRET`.

### Стъпка 3: Създай Admin Потребител

```bash
# Локално (препоръчително)
# Увери се, че .env има правилен DATABASE_URL
node scripts/create-admin.js
```

Въведи:
- Username: `admin` (или каквото искаш)
- Password: (запиши го!)
- Email: (опционално)

### Стъпка 4: Провери Logs

1. Render Dashboard → Logs
2. Рестартирай сървъра (Manual Deploy → Clear build cache & deploy)
3. Опитай да се логнеш
4. Виж логовете за грешки

### Стъпка 5: Тествай Login

1. Отиди на `https://your-app.onrender.com/login`
2. Въведи username и password
3. Провери дали работи

---

## 7. Ако все още не работи

### Проверка на Logs:

1. Render Dashboard → Logs
2. Скрол надолу до най-новите съобщения
3. Копирай грешките и ги провери

### Често срещани проблеми:

**"Database connection failed"**
- Провери `DATABASE_URL` в Render
- Провери дали Neon DB е достъпен
- Провери Neon DB connection settings

**"SESSION_SECRET is not set"**
- Добави `SESSION_SECRET` в Render Environment Variables
- Рестартирай сървъра

**"User not found" или "Invalid password"**
- Създай admin потребител (виж Стъпка 3)
- Провери дали username/password са правилни

**"Cannot set cookie"**
- Провери дали `NODE_ENV=production` е зададен
- Cookies работят само с HTTPS на Render (това е нормално)

---

## 8. Тестване локално

Преди да тестваш на Render, увери се, че работи локално:

```bash
# Локално
# Увери се, че .env има правилни стойности
npm start
```

Отвори `http://localhost:3000` и тествай login.

Ако работи локално, но не на Render → проблемът е в environment variables или конфигурацията на Render.

---

## Бърза проверка списък:

- [ ] `DATABASE_URL` е зададен в Render Environment Variables
- [ ] `SESSION_SECRET` е зададен в Render Environment Variables
- [ ] `NODE_ENV=production` е зададен в Render
- [ ] Имаш създаден admin потребител в базата данни
- [ ] Базата данни е достъпна (провери Neon DB)
- [ ] Render Logs няма критични грешки
- [ ] Приложението работи локално

---

## Получаване на помощ

Ако все още не работи:

1. Копирай логовете от Render (Dashboard → Logs)
2. Провери environment variables
3. Опиши какво точно се случва (каква грешка виждаш?)
4. Провери дали работи локално

Успех! 🚀

