# Бърз старт - Деплой на Render

## Стъпка 1: Подготовка на кода

✅ Кодът вече е готов! Не са нужни промени.

## Стъпка 2: Създай акаунт на Render

1. Отиди на https://render.com
2. Кликни "Get Started for Free"
3. Влез с GitHub акаунт (най-лесно)

## Стъпка 3: Свържи GitHub Repository

1. В Render Dashboard, кликни "New +"
2. Избери "Web Service"
3. Свържи GitHub акаунта (ако не е свързан)
4. Избери твоя repository (`ofertnik`)

## Стъпка 4: Конфигурация на Web Service

### Основни настройки:

- **Name:** `ofertnik`
- **Environment:** `Node`
- **Region:** `Frankfurt (EU)` или най-близкия EU регион
- **Branch:** `main` (или `master`, в зависимост от твоя branch)
- **Root Directory:** *(остави празно)*
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- **Plan:** `Free`

### Environment Variables:

Кликни "Advanced" → "Add Environment Variable" и добави:

| Key | Value | Забележка |
|-----|-------|-----------|
| `DATABASE_URL` | `postgresql://...` | Твоя Neon DB connection string |
| `SESSION_SECRET` | `[генерирай нов]` | Виж по-долу как да генерираш |
| `NODE_ENV` | `production` | Важно за sessions |

### Генериране на SESSION_SECRET:

**Локално (в терминала):**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

Копирай резултата и го използвай за `SESSION_SECRET`.

## Стъпка 5: Create Web Service

Кликни "Create Web Service"

Render ще:
1. Клонира repository-то
2. Инсталира зависимостите
3. Стартира сървъра
4. Деплойне приложението

**Време:** ~3-5 минути за първи път

## Стъпка 6: Тествай

След като deploy-ът завърши:
1. Render ще ти даде URL: `https://ofertnik.onrender.com` (или подобен)
2. Отвори URL-а в браузъра
3. Тествай всички функции

## Стъпка 7: (Опционално) Custom Domain

Ако искаш да използваш собствен домейн:

1. В Render Dashboard → твоя Web Service → Settings
2. Скрол до "Custom Domains"
3. Добави домейна
4. Следвай инструкциите за DNS настройки

---

## Важно за Neon DB

Увери се, че Neon DB позволява connections от Render IP адреси.

В Neon DB Dashboard:
- Отиди на твоя database
- Провери "Connection Settings"
- Обикновено по подразбиране е достъпен за всички (но провери)

---

## Автоматично обновяване

При всеки `git push`:
- Render автоматично ще деплойне новите промени
- Ще видиш progress в Render Dashboard
- Deploy отнема ~2-3 минути

---

## Често срещани проблеми

### "Build failed"
- Провери логовете в Render Dashboard → Logs
- Увери се, че `npm install` работи локално

### "Application error"
- Провери логовете (Logs tab)
- Провери environment variables (особено `DATABASE_URL` и `SESSION_SECRET`)

### "Database connection failed"
- Провери `DATABASE_URL` environment variable
- Увери се, че Neon DB е достъпен
- Провери Neon DB connection settings

### "Slow first load"
- Free tier "sleeps" след 15 мин inactivity
- Първото зареждане може да отнеме ~30 секунди (нормално)
- След това е бързо

---

## Полезни линкове

- Render Dashboard: https://dashboard.render.com
- Render Documentation: https://render.com/docs
- Neon DB: https://neon.tech

---

## Следващи стъпки

1. ✅ Деплойни на Render
2. ✅ Тествай приложението
3. ✅ (Опционално) Настрой custom domain
4. ✅ Премахни Netlify деплой (ако не го използваш)

Успех! 🚀

