# Как да качиш приложението в GitHub

## Стъпка 1: Създай GitHub Repository

1. Отиди на https://github.com
2. Влез в акаунта си (или създай нов)
3. Кликни "+" в горния десен ъгъл → "New repository"
4. Попълни:
   - **Repository name:** `ofertnik` (или друго име)
   - **Description:** `Калкулатор за застраховки - CASCO, ГО, GAP`
   - **Visibility:** Public или Private (избери според предпочитанието си)
   - **НЕ маркирай** "Add a README file", "Add .gitignore", или "Choose a license" (вече имаме тези файлове)
5. Кликни "Create repository"

## Стъпка 2: Инициализирай Git локално

Отвори терминала в директорията на проекта и изпълни:

```bash
# Инициализирай git repository
git init

# Добави всички файлове
git add .

# Направи първи commit
git commit -m "Initial commit: Офертник - Калкулатор за застраховки"
```

## Стъпка 3: Свържи с GitHub

GitHub ще ти покаже команди след като създадеш repository. Обикновено са:

```bash
# Добави remote repository (замени USERNAME и REPO_NAME)
git remote add origin https://github.com/USERNAME/REPO_NAME.git

# Промени главния branch на main (ако е нужно)
git branch -M main

# Качи кода в GitHub
git push -u origin main
```

**Важно:** Замени `USERNAME` и `REPO_NAME` с твоето GitHub потребителско име и името на repository-то!

## Стъпка 4: Проверка

1. Отиди на твоя GitHub repository
2. Провери дали всички файлове са качени
3. Увери се, че `.env` файлът НЕ е качен (трябва да е в .gitignore)

---

## Следващи стъпки (за Render)

След като кодът е в GitHub:

1. Отиди на https://render.com
2. Създай Web Service
3. Свържи GitHub repository
4. Render автоматично ще открие приложението
5. Добави environment variables
6. Deploy!

---

## Важно: Environment Variables

`.env` файлът НЕ трябва да се качва в GitHub (вече е в .gitignore).

Когато деплойнеш на Render, трябва да добавиш environment variables в Render Dashboard:
- `DATABASE_URL`
- `SESSION_SECRET`
- `NODE_ENV=production`

---

## Полезни Git команди

```bash
# Провери статус
git status

# Добави файлове
git add .

# Направи commit
git commit -m "Описание на промените"

# Качи в GitHub
git push

# Изтегли промени от GitHub
git pull

# Виж история на commits
git log
```

---

## Ако имаш проблеми

### "Permission denied"
- Провери дали си влязъл в GitHub
- Може да се нуждаеш от Personal Access Token вместо парола

### "Repository not found"
- Провери дали URL-ът е правилен
- Провери дали имаш права за достъп до repository-то

### ".env file is tracked"
- `.env` не трябва да е в GitHub
- Ако вече е качен:
  ```bash
  git rm --cached .env
  git commit -m "Remove .env file"
  git push
  ```

Успех! 🚀

